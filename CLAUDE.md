# ndt-factory-cloud — Agent Instruction

All agents must begin by executing the boot sequence defined in PROJECT_BOOTSTRAP.md.

Do not proceed with any implementation, code generation, or file modification until
the boot sequence has been completed and the Engineering OS context is fully loaded.

This rule is absolute. There are no exceptions.
